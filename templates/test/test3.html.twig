{% extends 'base.html.twig' %}

{% block title %}Hello TestController!{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>Hello</h1>

        {% for post in posts %}
            <h2>{{ post.title }}</h2>
            <h3>{{ post.category.myToString }}</h3>
        {% endfor %}
    
</div>
{% endblock %}
